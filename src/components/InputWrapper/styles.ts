import styled from "styled-components";

export const Container = styled.div`
    display: flex;
    flex-direction: row;
    align-items: center;
    gap: 3rem;
    height: 7rem;
    width: 60rem;
    border: 1px solid red;
`
