import styled from "styled-components";

export const Container = styled.button`
    height: 4rem;
    width: 4rem;

    background-color: ${({ theme }) => theme.colors.GREEN_BUTTON};
    border: none;
    border-radius: 1rem;
`
